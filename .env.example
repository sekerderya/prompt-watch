DATABASE_URL=postgresql://promptwatch:promptwatch@localhost:5432/promptwatch
OPENAI_API_KEY=
PROMPTWATCH_BACKEND_URL=http://localhost:3000

# Leave empty and auth stays OFF (the default for local development and the demo).
# Set it before ANY shared or internet-facing deployment; docker-compose passes
# this straight through to the backend container.
#   PROMPTWATCH_API_KEY=$(openssl rand -hex 32)
PROMPTWATCH_API_KEY=
